version: 1
frontend:
  phases:
    build:
      commands:
        - npm ci
        - cd frontend && npx next build
  artifacts:
    baseDirectory: frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - frontend/.next/cache/**/*
